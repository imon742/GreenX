import { getServices } from '@/lib/data'; import { ServiceCard } from '@/components/Cards'; import SectionTitle from '@/components/SectionTitle';
export const metadata={title:'Solutions'};
export default async function Solutions(){const services=await getServices();return <><section className="page-hero"><div className="container"><span className="eyebrow light">Solutions</span><h1>Integrated engineering for power and infrastructure.</h1><p>Explore Green X solution areas, then contact the team for a site-specific proposal.</p></div></section><section className="section"><div className="container"><SectionTitle eyebrow="Capabilities" title="One portfolio, multiple engineering disciplines."/><div className="service-grid">{services.map((s,i)=><ServiceCard key={s.slug} item={{...s,display_order:i+1}}/>)}</div></div></section></>}
