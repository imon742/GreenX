import { notFound } from 'next/navigation'; import Image from 'next/image'; import Link from 'next/link'; import { getService } from '@/lib/data';
export default async function Solution({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await getService(slug);if(!s)notFound();return <><section className="page-hero compact"><div className="container"><span className="eyebrow light">Green X Solution</span><h1>{s.title}</h1><p>{s.short_description}</p></div></section><section className="section"><div className="container detail-grid"><div className="detail-visual"><Image src={s.image_url||'/assets/electrical.svg'} alt={s.title} fill sizes="45vw"/></div><div><span className="eyebrow">Engineering scope</span><h2>Built around your site, load and operating priorities.</h2><p className="lead">{s.description}</p><div className="feature-list">{(s.features||[]).map(x=><div key={x}><span>✓</span>{x}</div>)}</div><Link href="/contact#quote" className="btn btn-primary">Request a Proposal</Link></div></div></section></>}
