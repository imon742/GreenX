import { notFound } from 'next/navigation'; import Image from 'next/image'; import Link from 'next/link'; import { getProduct } from '@/lib/data';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getProduct(slug);if(!p)notFound();return <><section className="section product-detail"><div className="container detail-grid"><div className="detail-visual product"><Image src={p.image_url||'/assets/product-powerstation.svg'} alt={p.name} fill sizes="45vw"/>{p.demo&&<span className="sample-badge">Sample catalog item</span>}</div><div><span className="eyebrow">{p.category}</span><h1>{p.name}</h1><p className="lead">{p.short_description}</p>{p.demo&&<div className="notice">This sample item demonstrates the product-page design. Replace it with your actual supplied model before presenting it as Green X inventory.</div>}<h3>Key specifications</h3><div className="spec-table">{Object.entries(p.specifications||{}).map(([k,v])=><div key={k}><span>{k.replace(/([A-Z])/g,' $1')}</span><b>{v}</b></div>)}</div><p>{p.description}</p><div className="hero-actions"><Link className="btn btn-primary" href={`/contact#quote`}>Request Price / Proposal</Link>{p.brochure_url&&<a className="btn btn-secondary" href={p.brochure_url} target="_blank">Download Brochure</a>}</div></div></div></section></>}
