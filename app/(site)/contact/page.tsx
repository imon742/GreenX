import QuoteForm from '@/components/QuoteForm'; import { getSettings } from '@/lib/data';
export const metadata={title:'Contact'};
export default async function Contact(){const s=await getSettings();const wa=(s.whatsapp||s.phone||'').replace(/\D/g,'');return <><section className="page-hero"><div className="container"><span className="eyebrow light">Contact</span><h1>Bring us the requirement. We’ll help shape the engineering solution.</h1><p>For project enquiries, maintenance requests and product proposals across Bangladesh.</p></div></section><section id="quote" className="section"><div className="container quote-layout"><div className="contact-panel"><span className="eyebrow">Green X Power Engineering</span><h2>Talk to the team.</h2>{s.phone&&<a href={`tel:${s.phone}`}><small>Phone</small>{s.phone}</a>}{wa&&<a target="_blank" href={`https://wa.me/${wa}`}><small>WhatsApp</small>Chat on WhatsApp</a>}{s.email&&<a href={`mailto:${s.email}`}><small>Email</small>{s.email}</a>}<div><small>Office</small><p>{s.address}</p></div></div><QuoteForm/></div></section></>}
