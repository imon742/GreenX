import { notFound } from 'next/navigation'; import Image from 'next/image'; import Link from 'next/link'; import { getProject,getProjectMedia } from '@/lib/data';
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getProject(slug);if(!p)notFound();const media=await getProjectMedia(p.id);return <><section className="project-hero"><Image src={p.cover_image_url||'/assets/solar-project.svg'} alt={p.title} fill priority sizes="100vw"/><div className="project-hero-overlay"/><div className="container project-hero-copy">{p.demo&&<span className="sample-badge">Portfolio preview</span>}<span className="eyebrow light">{p.category}</span><h1>{p.title}</h1><p>{p.short_description}</p></div></section><section className="section"><div className="container project-detail-grid"><aside><div className="project-facts"><div><span>Client</span><b>{p.client_name||'Confidential'}</b></div><div><span>Location</span><b>{p.location}</b></div><div><span>Capacity / Scope</span><b>{p.capacity}</b></div><div><span>Completion</span><b>{p.completion_date||'—'}</b></div></div></aside><article><h2>Project overview</h2>{p.demo&&<div className="notice">This is sample presentation data for the initial client demo. Replace it from Admin with a Green X completed project before using it as a company claim.</div>}<p className="lead">{p.description}</p><h3>Services delivered</h3><div className="feature-list">{(p.services_provided||[]).map(x=><div key={x}><span>✓</span>{x}</div>)}</div><h3>Technical snapshot</h3><div className="spec-table">{Object.entries(p.technical_details||{}).map(([k,v])=><div key={k}><span>{k}</span><b>{v}</b></div>)}</div><Link className="btn btn-primary" href="/contact#quote">Discuss a Similar Project</Link></article></div>{media.length>0&&<div className="container gallery-grid">{media.map((m:any)=><div className="gallery-item" key={m.id}><Image src={m.media_url} alt={m.alt_text||p.title} fill sizes="33vw"/></div>)}</div>}</section></>}
