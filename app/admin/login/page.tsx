'use client'; import { FormEvent, useState } from 'react'; import { createClient } from '@/lib/supabase/client';
export default function AdminLogin(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('Signing in…');const f=new FormData(e.currentTarget);const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error){setMsg(error.message);return;}window.location.href='/admin';}return <main className="login-page"><div className="login-card"><img src="/assets/logo.svg" alt="Green X"/><span className="eyebrow">Admin CMS</span><h1>Manage the website without touching code.</h1><p>Projects, products, services, images, clients and enquiries are managed here.</p><form onSubmit={submit}><label>Email<input name="email" type="email" defaultValue="admin@google.com" required/></label><label>Password<input name="password" type="password" required/></label><button className="btn btn-primary btn-wide">Sign in</button></form>{msg&&<p className="form-status">{msg}</p>}<a href="/">← Back to website</a></div></main>}
