import Link from 'next/link'; import { SiteSettings } from '@/lib/types'; import { PRODUCT_CATEGORIES, NAV_SOLUTIONS } from '@/lib/constants';
export default function Footer({settings}:{settings:SiteSettings}) { const wa=(settings.whatsapp||settings.phone||'').replace(/\D/g,''); return <footer className="footer"><div className="container footer-grid"><div><div className="footer-brand">GREEN <b>X</b></div><p>{settings.tagline}</p><p className="muted">{settings.address}</p></div><div><h4>Solutions</h4>{NAV_SOLUTIONS.slice(0,5).map(([l,s])=><Link key={s} href={`/solutions/${s}`}>{l}</Link>)}</div><div><h4>Products</h4>{PRODUCT_CATEGORIES.slice(0,5).map(c=><Link key={c} href={`/products?category=${encodeURIComponent(c)}`}>{c}</Link>)}</div><div><h4>Contact</h4>{settings.phone&&<a href={`tel:${settings.phone}`}>{settings.phone}</a>}{settings.email&&<a href={`mailto:${settings.email}`}>{settings.email}</a>}{wa&&<a target="_blank" rel="noreferrer" href={`https://wa.me/${wa}`}>WhatsApp</a>}<Link href="/contact#quote">Request a Quote</Link>{settings.facebook_url&&<a target="_blank" rel="noreferrer" href={settings.facebook_url}>Facebook</a>}{settings.linkedin_url&&<a target="_blank" rel="noreferrer" href={settings.linkedin_url}>LinkedIn</a>}{settings.youtube_url&&<a target="_blank" rel="noreferrer" href={settings.youtube_url}>YouTube</a>}</div></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} Green X Power Engineering.</span><span>Powering the Future, Sustainably.</span></div></footer> }
