'use client';
import { FormEvent, useState } from 'react';
export default function QuoteForm(){ const [status,setStatus]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Sending…'); const form=new FormData(e.currentTarget); const body=Object.fromEntries(form.entries()); const r=await fetch('/api/enquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}); if(r.ok){setStatus('Thank you. Your request has been received.'); e.currentTarget.reset();} else setStatus('Could not send the request. Please call or WhatsApp us.');} return <form className="quote-form" onSubmit={submit}><div className="field-grid"><label>Your name<input required name="name" /></label><label>Company<input name="company" /></label><label>Phone<input required name="phone" /></label><label>Email<input type="email" name="email" /></label><label>Service<select name="service"><option>Solar Energy</option><option>Generator & Backup Power</option><option>Lift & Elevator</option><option>Electrical Engineering</option><option>Lightning Protection</option><option>Maintenance & Support</option></select></label><label>Project location<input name="location" /></label></div><label>Tell us about your requirement<textarea name="message" rows={5} /></label><button className="btn btn-primary btn-wide" type="submit">Request Engineering Consultation</button>{status&&<p className="form-status">{status}</p>}</form> }
